# Core (always needed)
numpy>=1.24
opencv-python-headless>=4.8
pillow>=10.0

# --- OCR backend: install ONE of the three blocks below ------------------
# 1) RECOMMENDED — PaddleOCR PP-OCRv5: best free accuracy on tiny/blurry
#    text, runs well on CPU. (~600 MB of wheels, worth it.)
paddleocr>=3.1.0
paddlepaddle>=3.0.0

# 2) Lightweight alternative — same PP-OCR model family on ONNX Runtime:
# rapidocr>=2.0.0
# onnxruntime>=1.17

# 3) Universal fallback — also install the tesseract binary
#    (sudo apt install tesseract-ocr / brew install tesseract):
# pytesseract>=0.3.10
