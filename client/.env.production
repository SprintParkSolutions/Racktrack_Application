# Same-origin: the client is served through the API host / tunnel proxy, so
# /api, /uploads, /outputs resolve against the origin the app loaded from.
# Set an absolute https:// URL only if the API is on a different public host.
VITE_API_BASE=
